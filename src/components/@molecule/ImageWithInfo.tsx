// @flow
import React, {useState, useEffect} from 'react';

type Props = {
    
};
const ImageWithInfo = (props: Props) => {
    return (
        <div>

        </div>
    );
};

export default ImageWithInfo;

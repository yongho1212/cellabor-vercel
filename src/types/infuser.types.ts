export interface UserType {
    profileImg? : string,
    name? : string,
    email? : string,
    phone? : string,
    sex? : string,
    birth? : string,
    role?: string,
    description?: string,
    location?: string,
    category?: string[],
}
